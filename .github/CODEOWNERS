# This repo is a fork of DerekStride/tree-sitter-sql with full git history
# preserved, so GitHub's Contributors graph lists every upstream author from
# before the fork even though they never touched this rework - that list is
# just commit authorship, not repo access or review rights. This file is the
# actual, current ownership. See "Fork history & attribution" in README.md
# for the full story.

* @RelativelyUnknown
