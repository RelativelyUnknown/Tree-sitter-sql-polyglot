================================================================================
APPROXIMATE COUNT DISTINCT
================================================================================

SELECT APPROXIMATE COUNT(DISTINCT user_id) FROM events

--------------------------------------------------------------------------------

(program
  (statement
    (select
      (keyword_select)
      (select_expression
        (term
          (approximate_count
            (keyword_approximate)
            (invocation
              (object_reference
                (identifier))
              (keyword_distinct)
              (term
                (field
                  (identifier))))))))
    (from
      (keyword_from)
      (relation
        (object_reference
          (identifier))))))
