; Intent-based tests: expected trees derived from reading the grammar rules,
; not copied from parser output.
;
; mariadb/grammar/versioning.js overrides create_table to add:
;   optional(seq(keyword_with, keyword_system, keyword_versioning))
;
; These three keywords appear as direct children of create_table; there is
; no wrapper node for the WITH SYSTEM VERSIONING clause.  The keywords appear
; after the column_definitions.

================================================================================
MariaDB: CREATE TABLE WITH SYSTEM VERSIONING
================================================================================

CREATE TABLE events (id INT) WITH SYSTEM VERSIONING;

--------------------------------------------------------------------------------

(program
  (statement
    (create_table
      (keyword_create)
      (keyword_table)
      (object_reference
        name: (identifier))
      (column_definitions
        (column_definition
          name: (identifier)
          type: (int
            (keyword_int))))
      (keyword_with)
      (keyword_system)
      (keyword_versioning))))
